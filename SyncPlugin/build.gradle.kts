version = 1

cloudstream {
    language = "en"
    description = "Sync CloudStream settings, extensions and repositories across devices"
    authors = listOf("DjodyKort")
    status = 1 // 0=Down, 1=Ok, 2=Slow, 3=Beta
    tvTypes = listOf<String>() // Not a content provider
    iconUrl = "https://raw.githubusercontent.com/DjodyKort/cloudstream-sync/builds/icon.png"
}
